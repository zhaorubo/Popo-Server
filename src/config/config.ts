import { Config } from '../types/project';
let config: Config = {
    dataBase: {
        database: '',
        host: '',
        user: '',
        password: '',
        port: ''
    }
};
export default config;
